<%= cell_content do %>
  <div class="btn-group">
    <% @index_scenes.each do |ip| %>
      <%= link_to(ip.garden, scene_path(ip, :id => ip, :variety_type => ip.class._variety), :class => :btn) %>
    <% end %>
  </div>
<% end %>
