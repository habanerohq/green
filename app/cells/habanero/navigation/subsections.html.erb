<%= cell_content do %>
  <div class="btn-group">
    <% @index_pages.each do |ip| %>
      <%= link_to(ip.section, page_path(ip, :id => ip, :sorbet_type => ip.class._sorbet), :class => :btn) %>
    <% end %>
  </div>
<% end %>
