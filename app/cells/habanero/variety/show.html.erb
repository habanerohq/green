<%= cell_title(@target) %>

<%= cell_content do %>
  <dl>
    <% @traits.each do |i| %>
      <dt><%= i.name %></dt>
      <dd><%= format_trait(@target, i) %></dd>
    <% end %>
  </dl>
<% end %>

<% if @placement.feature.scene %>
  <%= link_to scene_path(@placement.feature.scene.edit_scene, @target), :class => 'btn' do %>
    <i class="icon-pencil"></i>
    Edit
  <% end %>
<% end %>